{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run pipeline (F5)",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/run_gait_pipeline.py",
      "console": "integratedTerminal",
      "env": {
        "PYTHONPATH": "${workspaceFolder}"
      }
    }
  ]
}
